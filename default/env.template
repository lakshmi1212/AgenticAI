# Template for environment variables
# Copy to .env and fill in your actual credentials (never commit secrets!)
LOGIN_URL=https://your-login-endpoint.example.com/api/login
LOGIN_EMAIL=your_email@example.com
LOGIN_PASSWORD=your_password