# Template for login automation environment variables
# Copy to .env and fill with actual values. NEVER commit secrets!
LOGIN_URL=https://your-login-endpoint.example.com/api/login
LOGIN_EMAIL=your-email@example.com
LOGIN_PASSWORD=your-secure-password
